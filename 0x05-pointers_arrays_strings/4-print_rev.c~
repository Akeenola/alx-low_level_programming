#include"main.h"
/**
 * print_rev - function name
 *
 *@s : parameter
 */
void	print_rev(char	*s)
{
	int	n	=	strlen(s);
	int	i;
	for(i	=	(n-1);	i	>=	0;	i--)
	{
		_putchar(s[i]);
	}
	_putchar('\n');
}
