#include"main.h"
#include<string.h>
/**
 *puts_half - function declaration
 *
 * @str : parameter
 */
void	puts_half(char	*str)
{
	int	len	=	strlen(str);
	int	half	=	len	/	2;
	for	(half	=	(len	/2);	half	<	len;	half++)
		_putchar(str[half]);
	_putchar('\n');
}
