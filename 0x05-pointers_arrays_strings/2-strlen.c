#include"main.h"
#include<string.h>
/**
 * _strlen - function on string length
 * @s : parameter
 * Return : length of a string
 */
int	_strlen(char	*s)
{
	char *tmp;
	tmp	=	s;
	return	(strlen(tmp));
}
