#include"main.h"
#include<string.h>
/**
 * _strlen - function on string length
 *
 * @s : parameter
 *
 * Return : int successful
 */
int	_strlen(char	*s)
{
	char *tmp;
	tmp	=	s;
	return	(strlen(tmp));
}
