/**
 *_islower- program to code leter cases
 *
 *int- char c
 */
#include"main.h"
int	_islower(int c)
{
	char	l	=	'a';
	int	d;
	while	(l	<	'z')
	{
		l++;
		if	(c	==	l)
		{
			d	=	0;
			break;
		}
	}
	if	(d	==	0)
		return	(1);
	else
		return	(0);
}
